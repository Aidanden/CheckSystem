# 🧪 صفحة اختبار SOAP والطباعة

تم إنشاء صفحة مخصصة لاختبار تكامل SOAP والطباعة بشكل مباشر ومنفصل عن باقي النظام.

## الرابط
`http://localhost:5000/soap-test`

## المميزات
1. **استعلام مباشر**: تستدعي الـ Backend الذي بدوره يستدعي خادم SOAP المحلي.
2. **عرض التفاصيل**: تعرض البيانات المسترجعة من SOAP (مثل تفاصيل الشيكات).
3. **طباعة فورية**: زر لطباعة الشيكات بناءً على البيانات المسترجعة.
4. **تشخيص الأخطاء**: تعرض رسائل خطأ واضحة في حالة الفشل.

## كيفية الاستخدام

1. **تأكد من تشغيل الخوادم**:
   - خادم SOAP: `npm run soap:test` (في `server/`)
   - الخادم الرئيسي: `npm run dev` (في `server/`)
   - العميل: `npm run dev` (في `client/`)

2. **افتح الصفحة**:
   - اذهب إلى `http://localhost:5000/soap-test`

3. **إجراء الاختبار**:
   - أدخل رقم الحساب (الافتراضي: `001001000811217`)
   - اضغط **"Query SOAP"**
   - انتظر ظهور النتائج.

4. **الطباعة**:
   - إذا ظهرت النتائج بنجاح، اضغط **"🖨️ Print Checkbook"**
   - ستفتح نافذة جديدة لمعاينة وطباعة الشيكات.

## ملاحظات فنية
- الصفحة تستخدم `accountService.query` الذي تم تكوينه لاستخدام SOAP.
- عند الطباعة، يتم إرسال `source: 'bank'` لضمان استخدام بيانات SOAP (أو Mock إذا فشل).
- يتم عرض `checkbookDetails` القادمة من SOAP بشكل JSON لتسهيل المراجعة.
