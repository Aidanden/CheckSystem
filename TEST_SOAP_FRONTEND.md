# 🧪 اختبار واجهة SOAP الأمامية

تم إنشاء واجهة مخصصة لاختبار SOAP والطباعة.

## الخطوات

1. **افتح التطبيق**:
   - اذهب إلى: `http://localhost:3040`
   - سجل الدخول (admin / 123)

2. **اذهب إلى صفحة الاختبار**:
   - من القائمة الجانبية، اختر **"اختبار SOAP"** (أو اذهب إلى `/soap-test`).

3. **إجراء الاختبار**:
   - رقم الحساب الافتراضي: `001001000811217`
   - اضغط **"Query SOAP"**
   - يجب أن تظهر تفاصيل الحساب والشيكات (باللون الأخضر).

4. **الطباعة**:
   - اضغط **"🖨️ Print Checkbook"**
   - ستفتح نافذة الطباعة مع البيانات الحقيقية من SOAP.

## ملاحظات
- الصفحة تتصل بالـ Backend الذي يتصل بـ SOAP Server المحلي (8080).
- إذا ظهر خطأ، تأكد من تشغيل `npm run soap:test` في مجلد `server`.
